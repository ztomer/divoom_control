[pytest]
# Keep tmp_path dirs only for FAILING tests, and only the last run.
# pytest's defaults (policy "all", count 3) retain every test's tmp_path for
# three runs. A sibling repo's fixture wrote a real 15GiB file per run and 26
# retained roots reached 246GB, filling a 926GB disk to 100% — after which
# unrelated gates failed for want of space rather than for anything in the diff.
tmp_path_retention_count = 1
tmp_path_retention_policy = failed
pythonpath = .
asyncio_mode = auto
# .claude/worktrees/ holds other agent sessions' git worktrees on shared dev
# machines — full copies of tests/, scripts/, divoomd/, each with the same
# module basenames as the real tree, which aborts collection with an "import
# file mismatch" the moment more than one worktree exists locally. CI never
# has this directory, but local runs on this shared-tree project regularly
# do.
# (archive/ used to be excluded here too — its conftest re-registered
# --run-hardware and aborted collection. The tree was removed in R66.)
# Merged from pyproject.toml's [tool.pytest.ini_options] on 2026-09-04. That
# block was DEAD: pytest says so out loud -- "configfile: pytest.ini (WARNING:
# ignoring pytest config in pyproject.toml!)" -- because an ini file always
# wins. It claimed to "match the existing pytest.ini" and did not: it declared
# minversion, testpaths, -ra and --strict-markers that this file never had, so
# every one of them was inert while reading as configured. One config now.
minversion = 8.0
testpaths = tests
addopts = -ra --strict-markers --ignore=.claude
norecursedirs = .claude
